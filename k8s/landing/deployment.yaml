apiVersion: apps/v1
kind: Deployment
metadata:
  name: wpp-landing
  namespace: wpp-bot
  labels:
    app: wpp-landing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wpp-landing
  template:
    metadata:
      labels:
        app: wpp-landing
    spec:
      containers:
        - name: wpp-landing
          image: ghcr.io/daymannovaes/wpp-landing:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
      imagePullSecrets:
        - name: ghcr-secret
