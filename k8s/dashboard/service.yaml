apiVersion: v1
kind: Service
metadata:
  name: wpp-dashboard
  namespace: wpp-bot
  labels:
    app: wpp-dashboard
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: wpp-dashboard
