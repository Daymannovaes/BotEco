---
const features = [
  {
    icon: '🎭',
    title: '15 Estilos de Voz',
    description: 'De vilão a narrador esportivo. Cada mensagem vira uma performance única.',
  },
  {
    icon: '⚡',
    title: 'Entrega Instantânea',
    description: 'Menos de 3 segundos para transformar texto em áudio estilizado.',
  },
  {
    icon: '👥',
    title: 'Funciona em Grupos',
    description: 'Use em conversas privadas ou grupos. Surpreenda toda a galera.',
  },
  {
    icon: '🇧🇷',
    title: '100% em Português',
    description: 'Vozes naturais em português brasileiro. Nada de sotaque estranho.',
  },
  {
    icon: '💾',
    title: 'Cache Inteligente',
    description: 'Mensagens repetidas? O áudio já está pronto. Economia e velocidade.',
  },
  {
    icon: '🗣️',
    title: 'Comandos Naturais',
    description: 'Fale como você fala. "Voz de robô", "tom sarcástico" - tudo funciona.',
  },
];
---

<section id="recursos" class="relative py-24 overflow-hidden">
  <!-- Background -->
  <div class="absolute inset-0 bg-gradient-to-b from-gray-950 via-gray-900 to-gray-950"></div>
  <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_bottom,_var(--tw-gradient-stops))] from-emerald-900/20 via-transparent to-transparent"></div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="inline-block bg-whatsapp-dark/20 border border-whatsapp-light/30 rounded-full px-4 py-1.5 text-whatsapp-light text-sm font-medium mb-4">
        Recursos
      </span>
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-4">
        Tudo que você precisa para <span class="bg-gradient-to-r from-whatsapp-light to-emerald-400 bg-clip-text text-transparent">impressionar</span>
      </h2>
      <p class="text-xl text-gray-400 max-w-2xl mx-auto">
        Tecnologia de ponta com a simplicidade que você merece.
      </p>
    </div>

    <!-- Features Grid -->
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
      {features.map((feature, index) => (
        <div
          class="group relative bg-gray-900/50 backdrop-blur border border-gray-800 hover:border-whatsapp-light/30 rounded-2xl p-6 transition-all duration-300 hover:shadow-xl hover:shadow-whatsapp-light/5"
          style={`animation-delay: ${index * 100}ms`}
        >
          {/* Gradient Overlay on Hover */}
          <div class="absolute inset-0 bg-gradient-to-br from-whatsapp-light/5 to-transparent opacity-0 group-hover:opacity-100 rounded-2xl transition-opacity duration-300"></div>

          <div class="relative">
            {/* Icon */}
            <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">
              {feature.icon}
            </div>

            {/* Content */}
            <h3 class="text-xl font-bold text-white mb-2">{feature.title}</h3>
            <p class="text-gray-400">{feature.description}</p>
          </div>
        </div>
      ))}
    </div>

    <!-- Bottom CTA -->
    <div class="mt-16 text-center">
      <p class="text-gray-400 mb-6">Pronto para começar?</p>
      <a
        href="/conectar"
        class="inline-flex items-center justify-center gap-2 bg-gradient-to-r from-whatsapp-light to-whatsapp-dark hover:from-whatsapp-dark hover:to-whatsapp-light text-white font-bold px-8 py-4 rounded-full text-lg transition-all duration-300 shadow-lg shadow-whatsapp-light/25 hover:shadow-whatsapp-light/40 hover:scale-105"
      >
        Começar Agora
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
    </div>
  </div>
</section>
