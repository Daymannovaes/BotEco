---
import AudioPlayer from './AudioPlayer.tsx';
import { voiceStyles } from '../data/voiceStyles';
---

<section id="demo" class="relative py-24 overflow-hidden">
  <!-- Background -->
  <div class="absolute inset-0 bg-gradient-to-b from-gray-950 via-gray-900 to-gray-950"></div>
  <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-whatsapp-dark/10 via-transparent to-transparent"></div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="inline-block bg-whatsapp-dark/20 border border-whatsapp-light/30 rounded-full px-4 py-1.5 text-whatsapp-light text-sm font-medium mb-4">
        Demonstrações
      </span>
      <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-4">
        Ouça os <span class="bg-gradient-to-r from-whatsapp-light to-emerald-400 bg-clip-text text-transparent">Estilos de Voz</span>
      </h2>
      <p class="text-xl text-gray-400 max-w-2xl mx-auto">
        Clique para ouvir como cada estilo transforma a mesma mensagem. Qual é o seu favorito?
      </p>
    </div>

    <!-- Voice Cards Grid -->
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-4">
      {voiceStyles.map((style) => (
        <div class="group bg-gray-900/50 backdrop-blur border border-gray-800 hover:border-gray-700 rounded-2xl p-5 transition-all duration-300 hover:shadow-xl hover:shadow-whatsapp-light/5 hover:-translate-y-1">
          <!-- Icon and Title -->
          <div class="flex items-center gap-3 mb-4">
            <span class="text-3xl">{style.icon}</span>
            <div>
              <h3 class="text-white font-semibold">{style.name}</h3>
              <p class="text-gray-500 text-xs">{style.description}</p>
            </div>
          </div>

          <!-- Audio Player -->
          <AudioPlayer
            client:idle
            src={style.audioFile}
            name={style.name}
            color={style.color}
          />
        </div>
      ))}
    </div>

    <!-- Note -->
    <p class="text-center text-gray-500 text-sm mt-8">
      Todas as demonstrações usam a mesma frase: "Olá! Sou o BotEco e vou transformar suas mensagens em algo extraordinário."
    </p>
  </div>
</section>
