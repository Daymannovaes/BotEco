---
---

<section class="relative py-24 overflow-hidden">
  <!-- Background -->
  <div class="absolute inset-0 bg-gradient-to-b from-gray-900 via-whatsapp-dark/20 to-gray-950"></div>
  <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-whatsapp-light/20 via-transparent to-transparent"></div>

  <!-- Floating Elements -->
  <div class="absolute top-1/4 left-1/4 w-64 h-64 bg-whatsapp-light/10 rounded-full blur-3xl animate-pulse"></div>
  <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-emerald-500/10 rounded-full blur-3xl animate-pulse delay-1000"></div>

  <div class="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <!-- Emoji -->
    <div class="text-6xl mb-6 animate-bounce">
      🤯
    </div>

    <!-- Headline -->
    <h2 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-white mb-6">
      Seus amigos vão perguntar:
      <br />
      <span class="bg-gradient-to-r from-whatsapp-light via-emerald-400 to-teal-400 bg-clip-text text-transparent">
        "Como você fez isso?!"
      </span>
    </h2>

    <!-- Subheadline -->
    <p class="text-xl text-gray-400 mb-10 max-w-2xl mx-auto">
      Transforme qualquer mensagem em uma performance épica. Comece agora e surpreenda todo mundo.
    </p>

    <!-- CTA Button -->
    <a
      href={import.meta.env.PUBLIC_DASHBOARD_URL}
      class="group inline-flex items-center justify-center gap-3 bg-gradient-to-r from-whatsapp-light to-whatsapp-dark hover:from-whatsapp-dark hover:to-whatsapp-light text-white font-bold px-10 py-5 rounded-full text-xl transition-all duration-300 shadow-2xl shadow-whatsapp-light/30 hover:shadow-whatsapp-light/50 hover:scale-105"
    >
      <svg class="w-7 h-7" fill="currentColor" viewBox="0 0 24 24">
        <path d="M12 2C6.48 2 2 6.48 2 12c0 1.74.45 3.37 1.23 4.79L2 22l5.35-1.21C8.71 21.55 10.32 22 12 22c5.52 0 10-4.48 10-10S17.52 2 12 2z"/>
      </svg>
      Começar Grátis Agora
      <svg class="w-6 h-6 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
      </svg>
    </a>

    <!-- Trust Points -->
    <div class="mt-8 flex flex-wrap items-center justify-center gap-6 text-sm text-gray-500">
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-whatsapp-light" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
        <span>Sem cartão de crédito</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-whatsapp-light" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
        <span>Começa em 30 segundos</span>
      </div>
      <div class="flex items-center gap-2">
        <svg class="w-5 h-5 text-whatsapp-light" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z" />
        </svg>
        <span>Feito com amor no Brasil</span>
      </div>
    </div>
  </div>
</section>
